# v25 consolidated update

This is a frontend consolidation of the previously broken UI layers. It specifically restores the route planner map, makes route metrics match the route drawn, preserves simultaneous alternatives, retains controlled InfoWindows, and keeps the isolated report table scrolling fix.

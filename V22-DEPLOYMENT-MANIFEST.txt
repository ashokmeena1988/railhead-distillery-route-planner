# LOGISTICS INTELLIGENCE SYSTEM — v22 FINAL

Replace these frontend files in GitHub Pages:

- index.html
- app.js
- styles.css
- README.md

DO NOT modify the Cloudflare Worker.

Four requested fixes retained/finalized:
1. Header branding and professional green/white color hierarchy.
2. Location pin uses compact Mappls InfoWindow.
3. Reports tables have an actual 460px vertical scroll viewport, visible scrollbar, all rows, and frozen header.
4. Route Planner keeps Mappls road-route alternatives with `path` + `paths` polyline compatibility.

After deployment:
1. Wait for GitHub Pages deployment.
2. Open the live site.
3. Hard refresh: Command + Shift + R.
4. Open Reports.
5. Generate Distillery Report.
6. A vertical scrollbar must be visible at the right edge of each report table.
7. Scroll it to reach all rows.
8. The green table header must remain frozen.

Cache version:
22.0.0
